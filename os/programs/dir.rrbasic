DIR$ = ARGS$
IF DIR$ = "dir" THEN DIR$ = ""
N = GETNAMECOUNT(DIR$)
FOR X = 0 TO N
	PRINT GETNAME$(DIR$,X);
	REPEAT
		PRINT " ";
	UNTIL CURRENTX > 32
	SIZE# = GETSIZE(DIR$,X)
	PRINT SIZE#
NEXT
IF DIR$ = "" THEN DIR$ = CSD$
PRINT N; " items in " + DIR$
END
