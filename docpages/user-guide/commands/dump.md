\page dump dump command

### dump [filename]
Outputs a hex dump of the provided file. The command will show 16 bytes of data per line, with the hex values on the left and the ASCII values on the right:

\image html dump.png

Unprintable characters will be shown as `.`

