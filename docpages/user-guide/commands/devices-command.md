\page devices-command devices command

```
devices
```

List all registered device names and their owning subsystems.

\image html devices.png

The output displays the device name in the left column and the subsystem which registered the device in the right column.

Device names are unique until the next reboot.
