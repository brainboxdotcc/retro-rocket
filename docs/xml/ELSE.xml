<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.9.1" xml:lang="en-US">
  <compounddef id="ELSE" kind="page">
    <compoundname>ELSE</compoundname>
    <title>ELSE Keyword</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><programlisting filename=".basic"><codeline><highlight class="normal">IF<sp/>expression<sp/>THEN<sp/>statement<sp/>ELSE<sp/>statement</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">IF<sp/>expression<sp/>THEN</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>statements</highlight></codeline>
<codeline><highlight class="normal">ENDIF</highlight></codeline>
<codeline></codeline>
<codeline><highlight class="normal">IF<sp/>expression<sp/>THEN</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>statements</highlight></codeline>
<codeline><highlight class="normal">ELSE</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>statements</highlight></codeline>
<codeline><highlight class="normal">ENDIF</highlight></codeline>
</programlisting></para>
<para><computeroutput>ELSE</computeroutput> provides the <bold>false branch</bold> of an <computeroutput>IF</computeroutput> decision. <linebreak/>
 When the <computeroutput>expression</computeroutput> evaluates to <bold>true</bold>, the <computeroutput>THEN</computeroutput> part runs; otherwise the <computeroutput>ELSE</computeroutput> part runs.</para>
<para><simplesect kind="remark"><para><computeroutput>ELSE</computeroutput> is used <bold>with</bold> <computeroutput>IF</computeroutput> and, in multi-line forms, must be paired with a matching <ref refid="ENDIF" kindref="compound">ENDIF</ref>. </para>
</simplesect>
<simplesect kind="remark"><para><computeroutput>ELSE</computeroutput> on its own is not a valid statement.</para>
</simplesect>
<hruler/>
</para>
<sect3 id="ELSE_1autotoc_md243">
<title>Forms</title>
<para><itemizedlist>
<listitem><para><bold>Single-line</bold> <linebreak/>
 <computeroutput>IF expression THEN statement ELSE statement</computeroutput> <linebreak/>
 Both the <computeroutput>THEN</computeroutput> and <computeroutput>ELSE</computeroutput> parts must be <bold>single statements</bold> on the same line.</para>
</listitem><listitem><para><bold>Multi-line without ELSE</bold> <linebreak/>
 <programlisting><codeline><highlight class="normal">IF<sp/>expression<sp/>THEN</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>statements</highlight></codeline>
<codeline><highlight class="normal">ENDIF</highlight></codeline>
</programlisting> The <computeroutput>statements</computeroutput> run only if <computeroutput>expression</computeroutput> is true.</para>
</listitem><listitem><para><bold>Multi-line with ELSE</bold> <linebreak/>
 <programlisting><codeline><highlight class="normal">IF<sp/>expression<sp/>THEN</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>statements-when-true</highlight></codeline>
<codeline><highlight class="normal">ELSE</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>statements-when-false</highlight></codeline>
<codeline><highlight class="normal">ENDIF</highlight></codeline>
</programlisting></para>
</listitem></itemizedlist>
</para>
<para><hruler/>
</para>
</sect3>
<sect3 id="ELSE_1autotoc_md245">
<title>Examples</title>
<para><bold>Single-line selection</bold> <programlisting filename=".basic"><codeline><highlight class="normal">PRINT<sp/>IF<sp/>X<sp/>&gt;<sp/>0<sp/>THEN<sp/>&quot;POSITIVE&quot;<sp/>ELSE<sp/>&quot;NON-POSITIVE&quot;</highlight></codeline>
</programlisting></para>
<para><bold>Multi-line with ELSE</bold> <programlisting filename=".basic"><codeline><highlight class="normal">IF<sp/>SCORE<sp/>&gt;=<sp/>100<sp/>THEN</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>PRINT<sp/>&quot;WELL<sp/>DONE!&quot;</highlight></codeline>
<codeline><highlight class="normal">ELSE</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>PRINT<sp/>&quot;KEEP<sp/>GOING...&quot;</highlight></codeline>
<codeline><highlight class="normal">ENDIF</highlight></codeline>
</programlisting></para>
<para><bold>Nested <computeroutput>IF</computeroutput>/<computeroutput>ELSE</computeroutput></bold> <programlisting filename=".basic"><codeline><highlight class="normal">IF<sp/>A<sp/>&gt;<sp/>B<sp/>THEN</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>PRINT<sp/>&quot;A<sp/>&gt;<sp/>B&quot;</highlight></codeline>
<codeline><highlight class="normal">ELSE</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>IF<sp/>A<sp/>=<sp/>B<sp/>THEN</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>PRINT<sp/>&quot;A<sp/>=<sp/>B&quot;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>ELSE</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>PRINT<sp/>&quot;A<sp/>&lt;<sp/>B&quot;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/>ENDIF</highlight></codeline>
<codeline><highlight class="normal">ENDIF</highlight></codeline>
</programlisting></para>
<para><hruler/>
</para>
</sect3>
<sect3 id="ELSE_1autotoc_md247">
<title>Rules &amp; behaviour</title>
<para><itemizedlist>
<listitem><para><bold>Pairing:</bold> In multi-line form, <computeroutput>ELSE</computeroutput> pairs with the <bold>nearest unmatched</bold> <computeroutput>IF</computeroutput>. Always terminate the block with <ref refid="ENDIF" kindref="compound">ENDIF</ref>.</para>
</listitem><listitem><para><bold>Single statement on one line:</bold> The one-line form allows exactly <bold>one</bold> statement after <computeroutput>THEN</computeroutput> and <bold>one</bold> after <computeroutput>ELSE</computeroutput>. To perform multiple actions, use the multi-line form.</para>
</listitem><listitem><para><bold>Nesting:</bold> <computeroutput>IF … THEN … ELSE … ENDIF</computeroutput> blocks may be nested freely.</para>
</listitem><listitem><para><bold>Whitespace:</bold> Keywords may be separated by spaces as shown; keep them on their own lines in multi-line form for clarity.</para>
</listitem></itemizedlist>
</para>
<para><bold>See also:</bold> <ref refid="IF" kindref="compound">IF</ref>, <ref refid="ENDIF" kindref="compound">ENDIF</ref> </para>
</sect3>
    </detaileddescription>
    <location file="docpages/basic-language-reference/keywords/ELSE.md"/>
  </compounddef>
</doxygen>
